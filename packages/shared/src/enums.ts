export enum UnunuraIdentifier {
  Padding = 'p',
  Margin = 'm',
  Border = 'border',
  Text = 'text',
  BackgroundColor = 'bg',
  BackgroundImage = 'bgi',
}

export enum UnunuraKeys {
  MultipleContextOpen = '[',
  MultipleContextClose = ']',
  SpecificContextOpen = '(',
  SpecificContextClose = ')',
  UniqueContext = ':',
  Important = '!',
}
