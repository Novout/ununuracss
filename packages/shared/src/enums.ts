export enum UnunuraIdentifier {
  Padding = 'p',
  Margin = 'm',
  Border = 'border',
  Text = 'text',
  BackgroundColor = 'bg',
  BackgroundImage = 'bgi',
  Radius = 'r',
}

export enum UnunuraKeys {
  MultipleContextOpen = '[',
  MultipleContextClose = ']',
  SpecificContextOpen = '(',
  SpecificContextClose = ')',
  UniqueContext = ':',
  Important = '!',
}
